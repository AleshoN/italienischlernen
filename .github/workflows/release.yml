name: Geprüftes Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version ohne führendes v, zum Beispiel 1.1.0'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm run lint
      - run: pnpm run format:check
      - run: pnpm run build
      - run: pnpm run release:verify
      - name: Releasepaket erstellen
        shell: bash
        run: |
          VERSION="${{ inputs.version }}"
          zip -r "piano-italienisch-${VERSION}.zip" \
            .github public scripts src \
            A1_ROADMAP.md CHANGELOG.md CONTENT_SOURCES_A1.md CURRICULUM_A1.md \
            OPEN_DATA.md README.md RELEASE_NOTES_*.md TESTS.md \
            eslint.config.js index.html package.json pnpm-lock.yaml \
            tsconfig.app.json tsconfig.json tsconfig.node.json vite.config.ts \
            dist
      - name: GitHub Release veröffentlichen
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: >-
          gh release create "v${VERSION}"
          "piano-italienisch-${VERSION}.zip"
          --target main
          --title "Piano ${VERSION}"
          --notes-file "RELEASE_NOTES_${VERSION}.md"
